
// event listeners

// get data from form

// check answers

// serialize data
function getSolutions() {
    let raw = document.getElementById('hidden-solutions');
    console.log(raw);
}

/**
 * This setup function is called when DOM loads.
 * It adds event listeners, initializes the game,
 * and collects solutions from html document.
 */
function setup () {
    getSolutions();
}